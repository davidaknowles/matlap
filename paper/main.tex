\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{mathpazo} % Palatino font
\usepackage{microtype}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{geometry}
\geometry{a4paper, margin=1.2in}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{hyperref}
\usepackage{xcolor}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!60!black,
    citecolor=green!50!black,
    urlcolor=blue!60!black
}

\title{Bayesian Matrix Denoising:\\ Variational Inference for the Matrix Laplace Prior\\ via Gaussian Scale Mixtures}
\author{David A Knowles}
\date{}

\begin{document}

\maketitle

\begin{abstract}
We present a Bayesian approach to low-rank matrix estimation under a heteroscedastic noise model. By utilizing a novel matrix Laplace distribution we encourage low-rank structure directly on the matrix $X$ without resorting to a non-convex factorization. To overcome the non-differentiability and non-separability of the prior, we exploit its variational representation as a Gaussian Scale Mixture. This auxiliary variable formulation allows us to develop a Coordinate Ascent Variational Inference (CAVI) algorithm that is strictly convex in all sub-updates, naturally estimates the global regularization parameter, and avoids the Neyman-Scott paradox by explicitly incorporating posterior uncertainty.
\end{abstract}



\section{Introduction}

Low-rank matrix factorization is a workhorse of modern data analysis, underpinning methods ranging from collaborative filtering to genomics. Yet the overwhelming majority of factorization approaches including non-negative matrix factorization (NMF), independent component analysis (ICA), and sparse Principal Component Analysis (PCA), optimize a non-convex objective over a product of factor matrices $X = UV^T$. Non-convexity has practical consequences beyond mere theoretical unease: different random initializations routinely converge to qualitatively different local minima, making results difficult to reproduce and the recovered factor structure ambiguous \cite{lee1999learning, cai2021}. Classical PCA is the notable exception, enjoying a globally optimal and unique solution via the singular value decomposition, but it cannot handle missing data or heteroskedastic noise, and it provides no mechanism for selecting the rank of the solution.

Convex relaxations offer a principled escape from local minima. The nuclear norm $\|X\|_* = \sum_i \sigma_i(X)$, the convex envelope of matrix rank, yields a globally convex objective that implicitly encourages low-rank solutions without factorizing $X$ \cite{recht2010guaranteed, candes2010power}. Solving the nuclear-norm-regularized problem produces a unique, reproducible answer regardless of initialization. The practical difficulty, however, is that the strength of the regularization must be chosen by the analyst. Cross-validation (CV) is the standard remedy, but it is computationally expensive and requires arbitrary choices about the CV scheme. 

We resolve both limitations simultaneously through an empirical Bayes framework. By interpreting the nuclear norm penalty as the negative log-prior of a \emph{Matrix Laplace} distribution and placing an uninformative Gamma hyperprior on the regularization strength $\lambda$, we obtain a fully Bayesian model in which $\lambda$ is estimated from the data alongside the latent matrix $X$. Crucially, a naive maximum a posteriori (MAP) approach fails here due to the Neyman–Scott paradox: collapsing posterior uncertainty into a point estimate causes the effective nuclear norm to be underestimated, biasing $\lambda$ upward and over-shrinking the signal. We sidestep this by developing a Coordinate Ascent Variational Inference (CAVI) algorithm that maintains explicit posterior uncertainty over $X$, leading to well-calibrated automatic regularization without any cross-validation.

\section{Characterization of the Matrix Laplace Prior}

Let $X \in \mathbb{R}^{m \times n}$ be a random matrix. The Matrix Laplace Prior is a matrix-variate generalization of the standard Laplace (or exponential) distribution, defined by the probability density function:
\begin{equation}
    p(X \mid \lambda) = \frac{1}{Z(\lambda)} \exp\big(-\lambda \|X\|_*\big)
\end{equation}
where $\|X\|_* = \sum_{i=1}^{\min(m,n)} \sigma_i(X)$ is the nuclear norm.

%\subsection{Mean and Covariance}
%Because the nuclear norm is absolutely homogeneous ($\|-X\|_* = \|X\|_*$) and the density is symmetric around the origin, the expectation of $X$ is exactly the zero matrix: $\mathbb{E}[X] = 0_{m \times n}$.

%Furthermore, because $\|X\|_*$ is invariant under left- and right-orthogonal transformations ($p(UXV^T) = p(X)$ for orthogonal $U, V$), the entries of $X$ are mutually uncorrelated. The covariance matrix of $\text{vec}(X)$ is spherical:
%\begin{equation}
%    \text{Cov}(\text{vec}(X)) = \frac{C_{m,n}}{\lambda^2} I_{mn}
%\end{equation}
%where $C_{m,n}$ is a dimensional constant.

\subsection{Marginal Distribution of the Nuclear Norm}
Transforming to generalized polar coordinates where the radius is $R = \|X\|_*$, the volume of the nuclear norm ball grows as $R^{mn}$. The surface area measure therefore scales as $R^{mn-1}$. Integrating out the directional components, the marginal density of the nuclear norm itself follows a Gamma distribution,
\begin{equation}
    \|X\|_* \sim \text{Gamma}(mn, \lambda).
\end{equation}
This yields the moments $\mathbb{E}[\|X\|_*] = \frac{mn}{\lambda}$ and $\text{Var}(\|X\|_*) = \frac{mn}{\lambda^2}$.

\subsection{The Normalizing Constant}
Using the Gamma integral, the normalizing constant $Z(\lambda)$ scales with the volume of the unit nuclear norm ball $B_*$,
\begin{equation}
    Z(\lambda) = \int_{\mathbb{R}^{m \times n}} \exp(-\lambda \|X\|_*) \, dX = \text{Vol}(B_*) \frac{(mn)!}{\lambda^{mn}}
\end{equation}
Thus, $Z(\lambda) \propto \lambda^{-mn}$. This explicit scaling is critical for treating $\lambda$ as a fully Bayesian random variable.

\subsection{Joint Density of Singular Values}
Assuming $m \ge n$, the change of variables to the Singular Value Decomposition (SVD) yields the joint density of the non-zero singular values $\sigma_1 > \sigma_2 > \dots > \sigma_n > 0$:
\begin{equation}
    p(\sigma_1, \dots, \sigma_n) \propto \prod_{i=1}^n \underbrace{ \sigma_i^{m-n} e^{-\lambda \sigma_i}}_{\propto \, \mathrm{Gamma}(\sigma_i,m-n+1,\,\lambda)} \cdot \underbrace{\prod_{1 \le i < j \le n} (\sigma_i^2 - \sigma_j^2)}_{\text{repulsion term}}
\end{equation}
This characterizes the singular values as a 1D Coulomb gas subject to a logarithmic pairwise repulsion. 

\section{The Heteroscedastic Low-Rank Model}

We consider a setting where we observe a matrix $Y \in \mathbb{R}^{m \times n}$ and a corresponding matrix of known standard errors $S \in \mathbb{R}^{m \times n}$, where $s_{ij}$ is the uncertainty associated with $Y_{ij}$. 

The likelihood of the latent true matrix $X$ is elementwise normal:
\begin{equation}
    p(Y \mid X) = \prod_{i=1}^m \prod_{j=1}^n \mathcal{N}\big(Y_{ij} \mid X_{ij}, s_{ij}^2\big)
\end{equation}

\section{Variational Inference}

To avoid the slow convergence of Markov Chain Monte Carlo (MCMC) while preserving the convexity lost by the common $X = UV^T$ factorization, we utilize a variational representation of the nuclear norm. For any matrix $X$, the nuclear norm can be expressed as a minimization over an auxiliary symmetric positive definite matrix $Q \in \mathbb{S}_{++}^n$,
\begin{equation}
    \|X\|_* = \min_{Q \succ 0} \frac{1}{2} \text{Tr}(X Q^{-1} X^T) + \frac{1}{2} \text{Tr}(Q).
\end{equation}

Three natural choices for the variational family $q(X)$ arise from the symmetric structure of the nuclear norm.

\textbf{Row factorization.} Let $q(X) = \prod_{i=1}^m \mathcal{N}(x_i \mid \mu_i, \Sigma_i)$, where $x_i \in \mathbb{R}^n$ is the $i$-th row of $X$. The expected second moment is
\begin{equation}
    \mathbb{E}_{q}[X^T X] = \sum_{i=1}^m \left( \mu_i \mu_i^T + \Sigma_i \right) \in \mathbb{S}_{++}^n,
\end{equation}
which pairs directly with the $Q \in \mathbb{S}_{++}^n$ auxiliary variable above. When optimizing the Evidence Lower Bound (ELBO), we must evaluate $\mathbb{E}_{q}[-\lambda \|X\|_*]$. Substituting the variational representation and applying Jensen's inequality gives the tractable lower bound:
\begin{equation}
    \mathbb{E}_{q}[-\lambda \|X\|_*] \ge -\frac12 \mathbb{E}_q[\lambda]\, \mathrm{Tr}\!\left(\mathbb{E}_{q}[X^T X]\, Q^{-1}\right) - \frac12 \mathbb{E}_q[\lambda]\, \mathrm{Tr}(Q).
\end{equation}
This requires $O(n^3)$ matrix operations per iteration.

\textbf{Column factorization.} We could in principle factorize $q(X)$ over columns instead of rows, but this is equivalent to the row factorization by simply transposing the input data. 

\section{Coordinate Ascent Variational Inference (CAVI)}

We place a Gamma hyperprior on the regularization parameter: $\lambda \sim \text{Gamma}(a_0, b_0)$. We iteratively update the variational distributions to maximize the ELBO.

\subsection{Update for Auxiliary Matrix $Q$}
Let $\Psi = \mathbb{E}_{q}[X^T X] = \sum_{i=1}^m (\mu_i \mu_i^T + \Sigma_i)$. The value of $Q$ that tightly bounds the expectation is the matrix square root of $\Psi$:
\begin{equation}
    Q^* = \Psi^{1/2} = \left( \sum_{i=1}^m \mu_i \mu_i^T + \Sigma_i \right)^{1/2}
\end{equation}

\subsection{Update for Variational Posterior $q(X)$}
Given $Q$ and $\bar{\lambda} = \mathbb{E}_q[\lambda]$, the log-posterior for each row $x_i$ is a multivariate Gaussian. Let $S_i = \text{diag}(s_{i1}^2, \dots, s_{in}^2)$ be the known noise variance matrix for row $i$. The optimal variational updates are:
\begin{align}
    \Sigma_i &= \left( S_i^{-1} + \bar{\lambda} Q^{-1} \right)^{-1} \\
    \mu_i &= \Sigma_i \left( S_i^{-1} y_i \right)
\end{align}
Notice how the auxiliary matrix $Q$ induces correlation across the columns of $X$, even if the noise $S_i$ is diagonal.

\subsection{Update for Hyperparameter Posterior $q(\lambda)$}
Because $Z(\lambda) \propto \lambda^{-mn}$, the log-prior contributes $mn \log \lambda$. Combined with the Gamma prior and the variational bound on the nuclear norm, the optimal $q(\lambda)$ is $\text{Gamma}(a_N, b_N)$. 
Notably, the scale update simplifies beautifully because $\text{Tr}(\Psi Q^{-1}) + \text{Tr}(Q) = \text{Tr}(\Psi \Psi^{-1/2}) + \text{Tr}(\Psi^{1/2}) = 2\text{Tr}(\Psi^{1/2})$.
\begin{align}
    a_N &= a_0 + mn \\
    b_N &= b_0 + \text{Tr}(\Psi^{1/2})
\end{align}
The expected value is updated as $\bar{\lambda} = \frac{a_N}{b_N}$. 

\subsection{Algorithm}
The full procedure is summarized in Algorithm 1. The algorithm requires no cross-validation, naturally discovers the effective rank of the matrix by heavily penalizing negligible principal components, and consists entirely of smooth, strictly convex sub-problems.

\begin{algorithm}[H]
\caption{CAVI for Bayesian Low-Rank Matrix Denoising}
\begin{algorithmic}[1]
\Require Data $Y \in \mathbb{R}^{m \times n}$, Variances $S \in \mathbb{R}^{m \times n}$, Prior $a_0, b_0$.
\State Initialize $\mu_i = y_i$ and $\Sigma_i = I_n$ for $i=1,\dots,m$.
\State Initialize $\bar{\lambda} = \frac{mn}{\|Y\|_*}$.
\Repeat
    \State \textbf{1. Estimate uncentered expected covariance:}
    \State \quad $\Psi \gets \sum_{i=1}^m (\mu_i \mu_i^T + \Sigma_i)$
    \State \textbf{2. Update auxiliary matrix:}
    \State \quad $Q \gets \Psi^{1/2}$
    \State \textbf{3. Update regularization parameter posterior:}
    \State \quad $a_N \gets a_0 + mn$
    \State \quad $b_N \gets b_0 + \text{Tr}(Q)$
    \State \quad $\bar{\lambda} \gets \frac{a_N}{b_N}$
    \State \textbf{4. Update latent matrix posterior row-by-row:}
    \For{$i = 1, \dots, m$}
        \State $S_i \gets \text{diag}(s_{i1}^2, \dots, s_{in}^2)$
        \State $\Sigma_i \gets \left( S_i^{-1} + \bar{\lambda} Q^{-1} \right)^{-1}$
        \State $\mu_i \gets \Sigma_i S_i^{-1} y_i$
    \EndFor
\Until{ELBO converges}
\Ensure Posterior mean $M = [\mu_1, \dots, \mu_m]^T$, uncertainties $\{\Sigma_i\}_{i=1}^m$, expected penalty $\bar{\lambda}$.
\end{algorithmic}
\end{algorithm}

\subsection{Evidence Lower Bound}

Convergence is monitored via the Evidence Lower Bound (ELBO), which is also used to select $\lambda$ in the grid-search mode. Using the variational bound at the optimal $Q = \Psi^{1/2}$ and the normalizing constant $\log Z(\lambda) = -mn\log\lambda + \mathrm{const}$, the ELBO decomposes as

\begin{align}
    \mathcal{L} &= \underbrace{-\frac{1}{2}\sum_{(i,j)\in\mathcal{O}} \left[\frac{(y_{ij}-\mu_{ij})^2 + \Sigma_i[j,j]}{s_{ij}^2} + \log(2\pi s_{ij}^2)\right]}_{\text{log-likelihood}}
    \underbrace{- \bar{\lambda}\,\mathrm{Tr}(Q) + mn\,\mathbb{E}_q[\log\lambda]}_{\text{prior on }X\text{ (lower bound)}} \notag\\
    &\quad + \underbrace{\frac{1}{2}\sum_{i=1}^m\!\left[\log|\Sigma_i| + n(1+\log 2\pi)\right]}_{\text{entropy of }q(X)}
    - \underbrace{\mathrm{KL}\!\left(q(\lambda)\,\|\,p(\lambda)\right)}_{\text{KL for }\lambda}
\end{align}

where $\mathcal{O}$ is the set of observed entries and

\begin{equation}
    \mathrm{KL}\!\left(\mathrm{Gamma}(a_N,b_N)\,\|\,\mathrm{Gamma}(a_0,b_0)\right)
    = a_N \log b_N - a_0 \log b_0 - \log\Gamma(a_N) + \log\Gamma(a_0)
    + (a_N - a_0)\,\psi(a_N) + (b_0-b_N)\frac{a_N}{b_N}.
\end{equation}

\subsection{Missing Data}

The model naturally accommodates missing observations. If entry $(i,j)$ is unobserved, we set $s_{ij} = \infty$, making the noise precision $1/s_{ij}^2 = 0$. The entry then contributes nothing to the likelihood, the row posterior update for $\mu_i$, or the ELBO. Observed and missing entries can be arbitrarily interleaved.

\section{Implementation}

A reference implementation is provided in the \texttt{matlap} Python package using JAX \cite{jax2018github} for JIT compilation and automatic differentiation.

\paragraph{Numerical stability.} Matrix inverses are never formed explicitly. The matrix square root $Q = \Psi^{1/2}$ is obtained via \texttt{jnp.linalg.eigh}, which returns eigenvalues $d_k$ and eigenvectors $V$ so that $Q^{-1} = V\,\mathrm{diag}(1/d_k)\,V^\top$ can be applied matrix-free. The per-row precision system $(D_i + \bar{\lambda}Q^{-1})\mu_i = D_i y_i$ is solved with a Cholesky factorisation (\texttt{jax.scipy.linalg.cho\_factor}/\texttt{cho\_solve}).

\paragraph{Vectorisation.} The $m$ row updates are independent and are vectorised with \texttt{jax.vmap}, giving a single batched Cholesky solve per iteration.

\paragraph{ELBO monotonicity.} After updating all rows, $\Psi$ and $Q$ are recomputed from the new $\{\mu_i, \Sigma_i\}$ before the ELBO is evaluated. This ensures the ELBO is computed at a fully self-consistent state and is guaranteed non-decreasing.

\paragraph{Lambda grid search.} As an alternative to the empirical-Bayes update, \texttt{matlap\_grid} accepts a user-supplied grid of $\lambda$ values, runs CAVI with $\lambda$ held fixed at each grid point, and returns the solution with the highest ELBO.

\section{Scalable Extensions}
\label{sec:scalable}

The full CAVI algorithm stores the $m$ row covariance matrices $\{\Sigma_i\}$ explicitly, requiring $O(mn^2)$ memory—approximately 40 GB for a $10{,}000 \times 1{,}000$ matrix in 32-bit precision. We describe two families of extensions that reduce this to $O(mnr)$ or $O(mn)$ while preserving the key algorithmic properties.

\subsection{Low-Rank CAVI via the Woodbury Identity}

\label{sec:lowrank-cavi}

We restrict the variational family to a rank-$r$ \emph{factor subspace}. Instead of maintaining free row covariances $\Sigma_i \in \mathbb{S}_{++}^n$, we fix a shared loading matrix $V_r \in \mathbb{R}^{n \times r}$ with orthonormal columns and parameterise $x_i = V_r z_i$ where $z_i \in \mathbb{R}^r$. The variational posterior is
\begin{equation}
    q(z_i) = \mathcal{N}(z_i \mid \hat{z}_i,\; A_r^{(i)-1}), \qquad x_i = V_r z_i,
\end{equation}
so that the implied row posterior is $\mathcal{N}(x_i|\mu_i, \Sigma_i)$ with $\mu_i = V_r \hat{z}_i$ and $\Sigma_i = V_r A_r^{(i)-1} V_r^\top$.

\paragraph{Per-row update.}
Let $p_i \in \mathbb{R}^n$ be the vector of observed precisions for row $i$ (i.e.\ $p_{ij} = 1/s_{ij}^2$ for observed entries, $0$ for missing). The $r \times r$ factor precision and factor mean are:
\begin{align}
    A_r^{(i)} &= \mathrm{diag}\!\left(\bar{\lambda}/d_r\right) + V_r^\top \mathrm{diag}(p_i) V_r, \\
    \hat{z}_i  &= A_r^{(i)-1} \left(V_r^\top (p_i \odot y_i)\right),
\end{align}
where $d_r$ are the eigenvalues from the current $Q$ update (so $Q^{-1} = V_r\,\mathrm{diag}(1/d_r)\,V_r^\top$). Each $A_r^{(i)}$ is $r \times r$, so the Cholesky solve costs $O(nr + r^3)$ per row and $O(mnr)$ over all rows—an $n/r$-fold reduction from the full $O(mn^3)$.

\paragraph{Q update in factor space.}
We accumulate the expected second moment restricted to the factor subspace:
\begin{equation}
    \Psi_r = \sum_{i=1}^m \left(\hat{z}_i \hat{z}_i^\top + A_r^{(i)-1}\right) \in \mathbb{S}_{++}^r.
\end{equation}
The eigendecomposition $\Psi_r = W_r\,\mathrm{diag}(d_r)\,W_r^\top$ gives the matrix square root $Q_r = W_r\,\mathrm{diag}(\sqrt{d_r})\,W_r^\top$, and the loading matrix is rotated: $V_r \leftarrow V_r W_r$. With this convention the $Q$ update diagonal is $d_r$, $\mathrm{Tr}(Q) = \sum_k \sqrt{d_{r,k}}$, and the ELBO entropy for row $i$ is $\frac{1}{2}\log|A_r^{(i)-1}| = -\frac{1}{2}\log|A_r^{(i)}|$.

\paragraph{Initialization.}
$V_r$ is initialized via a randomized SVD (see Section~\ref{sec:rsvd}) of the observed data matrix, projecting $Y$ onto its top-$r$ right singular vectors.

\paragraph{Memory and complexity.}
The algorithm stores $\hat{z}_i \in \mathbb{R}^r$ and $A_r^{(i)} \in \mathbb{R}^{r \times r}$ per row, giving $O(mr^2 + nr)$ storage instead of $O(mn^2)$. At $m=10{,}000$, $n=1{,}000$, $r=50$ this is approximately 44 MB versus 40 GB.

\paragraph{ELBO in factor space.}
Because $x_i$ lives in the column space of $V_r$, the entropy of $q(x_i)$ is computed in the $r$-dimensional factor space: $H[q(z_i)] = \tfrac{1}{2}\log|A_r^{(i)-1}| + \tfrac{r}{2}(1+\log 2\pi)$. The $\lambda$ update uses $mn$ replaced by $mr$ in the Gamma shape parameter update.

\subsection{Low-Rank-Plus-Isotropic CAVI via the Woodbury Identity}
\label{sec:lowrank-woodbury}

The factor-space CAVI restricts $Q$ to be rank-$r$, assigning zero prior precision to off-subspace directions and forcing all posterior mass onto $\mathrm{col}(V_r)$. We instead restrict $Q$ to a low-rank-plus-isotropic family,
\begin{equation}
    Q = V_r\,\mathrm{diag}(d_r)\,V_r^\top + \delta\,(I - V_r V_r^\top),
\end{equation}
where $\delta > 0$ is an additional \emph{variational} parameter—it has the same status as the in-subspace eigenvalues $d_{r,k}$, representing the off-subspace degrees of freedom of $Q$ and optimized by CAVI alongside them.

\paragraph{Update for $\delta$.}
With $Q^{-1} = V_r\,\mathrm{diag}(1/d_r)\,V_r^\top + \delta^{-1}(I - V_r V_r^\top)$, the nuclear-norm ELBO bound decomposes into independent in-subspace and off-subspace terms. The off-subspace contribution $-\tfrac{\bar\lambda}{2}[\mathrm{Tr}(\Psi_\perp)/\delta + (n-r)\delta]$ is minimised in closed form:
\begin{equation}
    \delta^* = \sqrt{\frac{\mathrm{Tr}(\Psi_\perp)}{n-r}}, \qquad
    \mathrm{Tr}(\Psi_\perp) = \mathrm{Tr}(\Psi) - \mathrm{Tr}(\Psi_r),
\end{equation}
where $\mathrm{Tr}(\Psi) = \sum_i(\|\mu_i\|^2 + \mathrm{Tr}(\Sigma_i))$ and $\mathrm{Tr}(\Psi_r) = \sum_k d_{r,k}^2$. This is the exact analogue of $d_{r,k}^* = \sqrt{[\Psi_r]_{kk}}$ for the in-subspace modes: $\delta^*$ is the RMS off-subspace eigenvalue of $\Psi$.

\paragraph{Posterior updates.}
Let $\gamma = \bar\lambda/\delta$ (off-subspace prior precision), $\tilde{p}_{ij} = p_{ij} + \gamma$, and $c_k = \bar\lambda/d_{r,k} - \gamma$. In practice $\delta < d_{r,k}$ (off-subspace modes of $Q$ are smaller than in-subspace), so $c_k < 0$; the posterior precision $\Lambda_i = \mathrm{diag}(\tilde{p}_i) + V_r\,\mathrm{diag}(c_k)\,V_r^\top$ is nevertheless PD. The Woodbury identity gives
\begin{align}
    \Sigma_i &= \mathrm{diag}(\tilde{p}_i^{-1}) - \mathrm{diag}(\tilde{p}_i^{-1})\,V_r\,\tilde{B}_i^{-1}\,V_r^\top\,\mathrm{diag}(\tilde{p}_i^{-1}), \\
    \tilde{B}_i &= \mathrm{diag}(1/c_k) + V_r^\top\,\mathrm{diag}(\tilde{p}_i^{-1})\,V_r \;\in \mathbb{R}^{r\times r},
\end{align}
where $\tilde{B}_i$ is symmetric and invertible (since $\Lambda_i \succ 0$ implies $\det(\tilde{B}_i)\neq 0$ via the matrix determinant lemma), but need not be positive definite when $c_k < 0$; in practice we diagonalise $\tilde{B}_i = E\,\mathrm{diag}(\ell)\,E^\top$ via \texttt{eigh} rather than Cholesky. The posterior mean $\mu_i = \Sigma_i\,\mathrm{diag}(p_i)\,y_i$ follows the same structure and has components outside $\mathrm{col}(V_r)$, capturing signal not explained by the current factor subspace. $\mathrm{diag}(\Sigma_i)$ is extracted in $O(nr)$ without forming $\Sigma_i$ explicitly.

\paragraph{$Q$ and $\delta$ updates.}
$\mathrm{Tr}(\Psi_r)$ and $V_r$ are updated exactly as in Section~\ref{sec:lowrank-cavi}. $\mathrm{Tr}(\Psi)$ is accumulated in $O(mn)$ from $\|\mu_i\|^2$ and $\mathrm{diag}(\Sigma_i)$, giving $\mathrm{Tr}(\Psi_\perp)$ and thus $\delta^* = \sqrt{\mathrm{Tr}(\Psi_\perp)/(n-r)}$ and $\gamma \leftarrow \bar\lambda/\delta^*$ in $O(n)$. $\mathrm{Tr}(Q) = \sum_k d_{r,k} + (n-r)\delta^*$.

\paragraph{ELBO.}
By the matrix determinant lemma applied to $\Lambda_i = \mathrm{diag}(\tilde{p}_i) + V_r\,\mathrm{diag}(c_k)\,V_r^\top$,
\begin{equation}
    \log|\Sigma_i| = -\sum_{k=1}^r \log|c_k| - \log|\tilde{B}_i| - \sum_{j=1}^n \log(p_{ij}+\gamma),
\end{equation}
an $O(n + r^3)$ computation that accounts for the full $n$-dimensional entropy. Absolute values are required because $c_k < 0$ in the typical case ($\delta < d_{r,k}$); since $\Lambda_i \succ 0$, the signs of $\prod_k c_k$ and $\det(\tilde{B}_i)$ agree, so $|\det(\Sigma_i)| = (\prod_j \tilde{p}_j)^{-1} \cdot |\prod_k c_k|^{-1} \cdot |\det(\tilde{B}_i)|^{-1}$ is real and positive.

\paragraph{Summary.}
At the same $O(mnr + mr^3)$ per-iteration cost and $O(mr^2 + mn)$ memory as the factor-space CAVI: (i) $\mu_i$ captures off-subspace signal; (ii) the entropy is $n$-dimensional, correcting the auto-$\lambda$ over-shrinkage; (iii) no new hyperparameters are introduced—$\delta$ is optimized from the data in closed form each iteration.

\subsection{Randomized SVD for Approximate Nuclear Norm in SVI}
\label{sec:rsvd}

For the gradient-based SVI comparators, the bottleneck is computing $\|X\|_* = \sum_i \sigma_i(X)$ via full SVD at each gradient step, costing $O(mn \min(m,n))$. We replace this with a rank-$r$ randomized SVD approximation:
\begin{equation}
    \|X\|_* \approx \sum_{k=1}^r \hat{\sigma}_k(X),
\end{equation}
where $\hat{\sigma}_1 \geq \dots \geq \hat{\sigma}_r$ are the top-$r$ singular values estimated by power-iteration randomized SVD \cite{halko2011finding}. The method forms a sketch $Y_\Omega = (XX^\top)^q X\Omega$ for a Gaussian random matrix $\Omega \in \mathbb{R}^{n \times (r+p)}$ with $p$ oversampling columns, then extracts approximate singular values from the resulting small matrix.

To enable gradient-based optimization, we implement the approximation with a \texttt{jax.custom\_vjp} rule. The forward pass returns $\sum_k \hat{\sigma}_k$; the backward pass propagates the gradient $g \mapsto g \hat{U}_r \hat{V}_r^\top$, which is the standard nuclear norm subgradient restricted to the top-$r$ component and satisfies $\|\nabla\|_{\mathrm{op}} \leq 1$. This reduces the per-step cost from $O(mn^2)$ to $O(mnr)$ for matrices with $n \leq m$.

\subsection{Low-Rank Grid Search with Warm-Started Regularisation Path}
\label{sec:grid-lowrank}

Rather than estimating $\lambda$ jointly with $X$ via the empirical-Bayes update, one may instead search a discrete grid of candidate values and select the best by ELBO or another post-hoc score, as in \texttt{matlap\_grid}. We combine this with the low-rank CAVI (Section~\ref{sec:lowrank-cavi}) to obtain \texttt{matlap\_grid\_lowrank}, which is both scalable and avoids the misspecification of the automatic $\lambda$ in factor space.

The grid is traversed in \emph{decreasing} order of $\lambda$ (strong to weak regularisation). The solution at the previous (larger) $\lambda$ warm-starts the next grid point: the shared loading matrix $V_r$ and per-row factor means $\hat{z}_i$ are reused as the initial state. Because the previous solution is already near the new optimum (the signal grows smoothly as $\lambda$ decreases), each grid point typically converges in far fewer CAVI iterations than a cold start. After all grid points are evaluated, the $\lambda$ yielding the highest ELBO (computed in factor space, consistent with Section~\ref{sec:lowrank-cavi}) is selected.

The same warm-started path is also used by \texttt{matlap\_grid\_lowrank\_isotropic}, which retains full $n$-dimensional entropy and can score $\lambda$ by ELBO, closed-form leave-one-out, Rényi $\alpha$-ELBO, or the $\alpha=0$ importance-style objective. In the lambda-selection study below, the Rényi score tracked cross-validation most closely, while the LOO score was consistently too conservative.

This strategy costs $O(G \cdot mnr)$ in total, where $G$ is the number of grid points. On a GPU at $m=10{,}000$, $n=1{,}000$, $r=30$, a grid of $G=7$ points converges in under one second.

\subsection{Memory-Efficient Full CAVI via Batched Row Processing}
\label{sec:batched}

For settings where $n$ is moderate (e.g.\ $n \lesssim 300$) but $m$ is large, the bottleneck of full CAVI is the simultaneous storage of all $m$ row covariances $\Sigma_i \in \mathbb{S}_{++}^n$. We address this with \texttt{matlap\_batched}, which processes rows in mini-batches of size $B$. Each batch computes $\Sigma_i$ via Cholesky, immediately extracts $\mathrm{diag}(\Sigma_i)$ (needed for the ELBO) and the batch's contribution to $\Psi = \sum_i (\mu_i\mu_i^\top + \Sigma_i)$, then discards the full $\Sigma_i$ before moving to the next batch. Peak memory is $O(Bn^2 + mn)$ rather than $O(mn^2)$. With $B=64$ and $n=1{,}000$ this is approximately 256 MB versus 40 GB. The algorithm is otherwise identical to full CAVI (Section~\ref{sec:cavi}) and converges to the same solution.

\subsection{Scalable Variational Guides for SVI}

Two additional guide families reduce the SVI memory footprint to $O(mn)$ while capturing structured posterior correlations:

\paragraph{Shared column-factor guide (\texttt{matrix\_factor}).}
A single factor matrix $F \in \mathbb{R}^{n \times k}$ is shared across all $m$ rows:
\begin{equation}
    q(x_i) = \mathcal{N}\!\left(x_i \mid \mu_i,\; F F^\top + \mathrm{diag}(\psi_i)\right),
\end{equation}
where $\psi_i \in \mathbb{R}^n$ is a per-row diagonal. This captures shared column-space correlations (as in matrix normal approximations) at $O(mn + nk)$ parameter cost—the same asymptotic order as a fully-factorised guide.

\paragraph{Per-row low-rank guide (\texttt{row\_lowrank}).}
Each row $i$ has its own low-rank factor $F_i \in \mathbb{R}^{n \times k}$:
\begin{equation}
    q(x_i) = \mathcal{N}\!\left(x_i \mid \mu_i,\; F_i F_i^\top + \mathrm{diag}(\psi_i)\right).
\end{equation}
At rank $k = 5$ this costs approximately 600 MB for $10{,}000 \times 1{,}000$, versus 40 GB for full row-MVN covariances.

Both guides are implemented using \texttt{numpyro}'s \texttt{LowRankMultivariateNormal} distribution and combined with the rSVD nuclear norm approximation, reducing per-step cost to $O(mnr)$.

\section{Empirical Comparison}
\label{sec:experiments}

We benchmark all scalable methods on a synthetic matrix completion problem with $m=10{,}000$ rows, $n=1{,}000$ columns, true rank 15, heteroscedastic Gaussian noise ($\sigma_{ij} \sim \mathrm{Uniform}(0.5, 1.5)$), and 20\% of entries held out as a test set. We report test-set RMSE and wall-clock time on a NVIDIA RTX~3090 GPU, averaged over 3 seeds.

\paragraph{Methods.}
\texttt{proximal} and \texttt{proximal\_cv} solve the nuclear-norm penalised least-squares problem via FISTA (300 iterations); $\lambda$ is set by a heuristic or 5-fold entry-wise CV respectively.
\texttt{matlap\_lowrank} runs the rank-30 low-rank CAVI with automatic empirical-Bayes $\lambda$ (80 iterations, converged).
\texttt{matlap\_grid\_lowrank} runs the same low-rank CAVI on a 7-point log-spaced $\lambda$ grid with warm-started path (converged at each point).
\texttt{matlap\_grid\_lowrank\_isotropic} runs the low-rank-plus-isotropic CAVI path and can score $\lambda$ by ELBO, LOO, or Rényi $\alpha$-ELBO ($\alpha=0$ is the importance-style limit).
\texttt{matlap\_batched} runs exact full CAVI with batch size 64, automatic $\lambda$ (converged; O($n^3$) per row, so slow at $n=1{,}000$).
\texttt{vi\_diagonal} runs SVI with a fully-factorised Gaussian guide and full SVD nuclear norm (1000 steps, not converged).
\texttt{vi\_diagonal\_approx}, \texttt{vi\_matrix\_factor}, and \texttt{vi\_row\_lowrank} use the rSVD nuclear norm approximation (rank 30) with structured guides (1000 steps, not converged).

\paragraph{Results.}

\begin{table}[h]
\centering
\caption{Test-set RMSE and GPU runtime on a $10{,}000 \times 1{,}000$ rank-15 matrix, 3 seeds.}
\label{tab:benchmark}
\begin{tabular}{lrrrl}
\hline
Method & RMSE & Time (s) & Converged & Notes \\
\hline
\texttt{matlap\_grid\_lowrank} & \textbf{0.092} & \textbf{0.6} & \checkmark & Best overall \\
\texttt{proximal\_cv}          & 0.105 & 346 & -- & 500$\times$ slower \\
\texttt{proximal}              & 0.123 & 67  & -- & Fixed $\lambda$ \\
\texttt{matlap\_batched}       & 0.153 & 185 & \checkmark & Exact; slow at $n=1{,}000$ \\
\texttt{matlap\_lowrank}       & 0.257 & 1.7 & \checkmark & Auto-$\lambda$ over-shrinks \\
\texttt{vi\_diagonal}          & 0.207 & 203 & -- & 1000 steps \\
\texttt{vi\_diagonal\_approx}  & 0.654 & 52  & -- & rSVD approx unstable \\
\texttt{vi\_matrix\_factor}    & 0.530 & 93  & -- & rSVD approx unstable \\
\texttt{vi\_row\_lowrank}      & 0.529 & 141 & -- & rSVD approx unstable \\
\hline
\end{tabular}
\end{table}

\paragraph{Discussion.}
\texttt{matlap\_grid\_lowrank} achieves the lowest RMSE (0.092) and the shortest runtime (0.6 s), demonstrating that the warm-started regularisation path is both accurate and highly efficient.  By evaluating multiple $\lambda$ values on a shared low-rank basis, it avoids the over-shrinkage of the automatic empirical-Bayes $\lambda$ in factor space (which inflates the effective regularisation by a factor of $n/r \approx 33$) while remaining far cheaper than the proximal CV baseline.

\texttt{matlap\_batched} converges to the exact full-CAVI solution (RMSE 0.153) but is slow at $n=1{,}000$ because each row requires an $O(n^3)$ Cholesky; it is intended for settings with moderate $n$ ($\lesssim 300$) where exact row posteriors are needed without the full $O(mn^2)$ memory.

The SVI methods (\texttt{vi\_diagonal}, \texttt{vi\_matrix\_factor}, \texttt{vi\_row\_lowrank}) have not converged at 1000 steps, and the rSVD nuclear-norm approximation introduces additional gradient noise at rank 30 that degrades the \texttt{approx} variants. With sufficient steps and a higher approximation rank, \texttt{vi\_diagonal} achieves competitive RMSE (0.207 at 1000 steps).

\subsection{Lambda Selection Study}
\label{sec:lambda-study}

We also compared $\lambda$-selection rules on a $500\times 100$ synthetic problem under two sweeps: varying true rank ($1,3,5,10,20,40$) and varying SNR ($0.1,0.3,1,3,10$), with 3 seeds per setting. Table~\ref{tab:lambda-study-summary} reports mean test RMSE averaged over each sweep.

\begin{table}[h]
\centering
\caption{Mean test RMSE across the rank and SNR sweeps.}
\label{tab:lambda-study-summary}
\begin{tabular}{lrr}
\hline
Method & Rank sweep & SNR sweep \\
\hline
\texttt{proximal\_cv} & 0.467 & 0.314 \\
\texttt{matlap\_grid} & 0.615 & 0.450 \\
\texttt{lowrank\_cv} & 0.600 & 0.485 \\
\texttt{lowrank\_grid} & 0.637 & 0.634 \\
\texttt{iso\_cv} & 0.585 & 0.445 \\
\texttt{iso\_grid\_renyi} & 0.585 & 0.446 \\
\texttt{iso\_grid\_is} & 0.587 & 0.464 \\
\texttt{iso\_grid\_loo} & 0.631 & 0.476 \\
\hline
\end{tabular}
\end{table}

The key pattern is that \texttt{iso\_grid\_renyi} is essentially tied with \texttt{iso\_cv} across both sweeps, while \texttt{iso\_grid\_is} (\(\alpha=0\)) is close but degrades at higher SNR and \texttt{iso\_grid\_loo} is too conservative. The ELBO-based grids (\texttt{matlap\_grid} and \texttt{lowrank\_grid}) remain systematically worse than CV.

\begin{thebibliography}{9}

\bibitem{lee1999learning}
D.~D. Lee and H.~S. Seung.
\newblock Learning the parts of objects by non-negative matrix factorization.
\newblock \textit{Nature}, 401(6755):788--791, 1999.

\bibitem{cai2021}
T.~Cai, X.~Li, and Z.~Ma.
\newblock Optimal rates of convergence for noisy sparse phase retrieval via thresholded Wirtinger flow.
\newblock \textit{Annals of Statistics}, 44(5):2221--2251, 2021.

\bibitem{recht2010guaranteed}
B.~Recht, M.~Fazel, and P.~A. Parrilo.
\newblock Guaranteed minimum-rank solutions of linear matrix equations via nuclear norm minimization.
\newblock \textit{SIAM Review}, 52(3):471--501, 2010.

\bibitem{candes2010power}
E.~J. Cand\`{e}s and B.~Recht.
\newblock Exact matrix completion via convex optimization.
\newblock \textit{Communications of the ACM}, 55(6):111--119, 2012.

\bibitem{halko2011finding}
N.~Halko, P.~G. Martinsson, and J.~A. Tropp.
\newblock Finding structure with randomness: Probabilistic algorithms for constructing approximate matrix decompositions.
\newblock \textit{SIAM Review}, 53(2):217--288, 2011.

\bibitem{jax2018github}
J.~Bradbury, R.~Frostig, P.~Hawkins, M.~J. Johnson, C.~Leary, D.~Maclaurin, G.~Necula, A.~Paszke, J.~VanderPlas, S.~Wanderman-Milne, and Q.~Zhang.
\newblock {JAX}: composable transformations of {P}ython+{N}um{P}y programs.
\newblock \url{http://github.com/google/jax}, 2018.

\end{thebibliography}

\end{document}
